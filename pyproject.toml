[project]
name = "issue-watcher"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gspread>=6.2,<7",
    "requests>=2.32,<3",
]

[dependency-groups]
dev = [
    "basedpyright",
    "ruff",
]

[tool.ruff]
include = ["scripts/*.py", "tests/*.py"]
